import styles from "@/styles/Home/index.module.scss"

export default function Home() {
  return (
    <main className={styles["main"]}>
      This is a website that displays extra features
    </main>
  )
}
